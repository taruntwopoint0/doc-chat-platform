-r requirements.txt
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
ruff>=0.8
# A real Postgres with pgvector, started in-process by the test suite and by
# scripts/dev_db.py -- no Docker needed. Dev only: never in the image.
pgserver>=0.1.4
